<controls:MetroWindow x:Class="WpfApplication2.SetGroupNameView"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
        xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
        xmlns:local="clr-namespace:WpfApplication2"
                        xmlns:controls="clr-namespace:MahApps.Metro.Controls;assembly=MahApps.Metro"
        mc:Ignorable="d"
        Title="SetGroupNameView" Height="70" Width="350" WindowStartupLocation="CenterScreen">
    <Grid>
        <StackPanel Orientation="Horizontal">
            <TextBlock VerticalAlignment="Center"  Margin="5">请设置名称</TextBlock>
            <TextBox VerticalAlignment="Center" Margin="10,0" Width="150" Height="20" Text="{Binding ThisGroup.Name}"></TextBox>
            <Button VerticalAlignment="Center" Command="{Binding OKCommand}"> OK</Button>
        </StackPanel>
    </Grid>
</controls:MetroWindow>
